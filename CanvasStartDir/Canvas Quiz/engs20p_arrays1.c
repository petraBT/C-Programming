#include <stdio.h>

int main(void) {
    int i;
	/* Please add your code here. 
	 * Be sure to declare your integer array with the name arr, and fill it with
	 * the values 2, 7, 4, 9, and -5.
	 */

    /* Do not change code below this line */
	for (i = 0; i < 5; i++){
	    printf("%d ", arr[i]);
	}  
	
	return(0);
}