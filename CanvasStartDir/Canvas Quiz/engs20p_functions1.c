#include <stdio.h>

/* insert prototype here */

int main(void) {
  int a;
  
/* insert function call here. */
  
  printf("a = %d. ", a);
  return(0);
}

int myaddition(int x, int y)
{
     return(x+y);
}