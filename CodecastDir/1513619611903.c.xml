<?xml version='1.0'?>
<cme>
#include &lt;stdio.h&gt;
int main(void) {
    int N=5;
    int i, fac;
    /* += -= *= /= %= */
    i = 1;
    fac = 1;
    while (i&lt;=N) {
        fac *= i; /* same as fac = fac * i; */
        i += 1; /* same as i = i + 1; */
    }
    
    printf("\n%d! = %d. \n", N, fac);
    return(0);
}
</cme>
