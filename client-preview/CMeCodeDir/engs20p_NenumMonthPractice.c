#include <stdio.h>

int main(void){
	/* Declare an enum "month" with all 12 months here */

	int userInput;

	printf("Enter the number of the current month (1-12): ");
	scanf("%d", &userInput);

	/* Cast userInput into your enum type */

	/* Write a switch statement here that prints "It's the summer"
	   for June, July, August, September, and "It's not summer" otherwise */

	return 0;
}
