<?xml version='1.0'?>
<cme>
#include &lt;stdio.h&gt;

/* prototypes */

int main(void) {
    int input, answer;
    printf("Which Fibonacci number would you like? ");
    scanf("%d", &amp;input);
    answer = fib(input);
    printf("The %dth Fibonacci number is %d.\n", input, answer);
    return(0);
}

/* functions go here */
</cme>
