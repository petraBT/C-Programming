#include <stdio.h>

/* Don't forget your prototype. */

int main(void) {
  int num;
  
/* Insert your code here.
 * Be sure to initialize num
 * and print its value before
 * and after the function call. */
  
  return(0);
}

void increasebyone(int *a)
{
      *a = *a + 1;
}
