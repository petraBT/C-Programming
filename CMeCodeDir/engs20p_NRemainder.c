#include <stdio.h>

int main(void) {

    int num, denom, remainder;
    
    printf("Please enter a numerator and a denominator, separated by a space: ");
    scanf("%d %d", &num, &denom);
    
    remainder = /* please complete this line */
    
    printf("The remainder, when dividing %d by %d, is %d.\n", num, denom, remainder);
    
    return(0);
}
